<%= form_with(model: tg_user) do |form| %>
  <% if tg_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tg_user.errors.count, "error") %> prohibited this tg_user from being saved:</h2>

      <ul>
        <% tg_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
